using System;

namespace MyNamespace
{
    public class MyClass
    {
        public static Vector3 AimChange()
        {
            var calculatedThing = CalculateNumbers((63f - 3f * weapon_Stats.Range - FPSCamera.FoV) * ((888 / 1) + 0)
            , FPSCamera.FoV / 100000f
            , 1f)
            .PrintResults(((FPSCamera.FoV < 15f) ? 15f : FPSCamera.FoV), "myString");
            var otherResultsVariable = calculatedThing.PrintOtherResults();
            OtherResult declaredVariable;
            declaredVariable.PrintOtherResults();
            Result instanceBuilder = new(otherResultsVariable);

            var typeCastedVariable = (OtherResult) declaredVariable.PrintOtherResults();
            typeCastedVariable.PrintOtherResults();
        }
        public Result CalculateNumbers(int num1, float num2, double num3)
        {

        }
    }

    public class Result
    {
        public Result(Result previousResult)
        {

        }
        public OtherResult PrintResults(double value, string stringValue)
        {
        }
    }
    public class OtherResult
    {
        public Result PrintOtherResults()
        {
        }
    }
}
