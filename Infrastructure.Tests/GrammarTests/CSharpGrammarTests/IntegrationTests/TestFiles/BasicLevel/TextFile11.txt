using System;

namespace MyNamespace
{
    public class Program
    {
        public static void Main()
        {
            // The "myWeapon" variable simulates a variable that couldn't be defined
            CalculateNumbers((63f - 3f * myWeapon.GetRange() - FPSCamera.FoV()) * ((888 / 1)
             + GetConstantTime())
            , FPSCamera.myValue / 100000f
            , operatorBB())
            .PrintResults(((FPSCamera.FoV() < 15f + FPSCamera.GetCameraValue()) ? 15f : Weapon.GetID()), "myString") 
                - operatorBB() + operatorCC(conflicterFunction()).conflicterFunction();
        }
        public Result CalculateNumbers(int num1, float num2, double num3)
        {
        }
        private float GetConstantTime()
        {
            return 13f;
        }
        private int operatorBB()
        {
            return 21;
        }
        private FPSCamera operatorCC(float data)
        {
            return 22f;
        }
        private float conflicterFunction()
        {
            return 23f;
        }
    }
    public class Weapon
    {
        public int GetRange()
        {
            return 11;
        }
        public static int GetID()
        {
            return 112193;
        }
    }
    public class FPSCamera
    {
        public int myValue;
        public static int FoV()
        {
            return 909;
        }
        public static int GetCameraValue()
        {
            return -11;
        }
        private double conflicterFunction()
        {
            return 24.0;
        }
    }
    public class Result
    {
        public OtherResult PrintResults(double value, string stringValue)
        {
        }
    }
}
